import dbPreorder from './preorder.js';

const quickDelivery = [];

quickDelivery.push(dbPreorder[dbPreorder.length - 1]);

quickDeleivery.promoName = 'Quick Delivery';

dbPreorder.pop();

export default quickDeleivery;
